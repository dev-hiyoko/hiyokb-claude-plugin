# OS
.DS_Store
Thumbs.db

# Python（スクリプトのキャッシュ）
__pycache__/
*.py[cod]
.venv/
venv/

# エディタ
.idea/
*.swp
.vscode/

# Claude Code のローカル設定（マシン依存・各自で持つ）
.claude/settings.local.json

# 環境変数・秘密
.env
.env.*

tmp

# 注: 実行時の知識ストアは ~/.task（ホーム配下）にあり、このリポジトリには含まれない。
#     ~/.task 側の機密除外は ~/.task/.gitignore を参照。
